# 2026-04-25 工作记录

## 微博热搜分析系统前端全面升级

### 完成内容
1. **引入 vue-router**：package.json 添加 vue-router ^4.4.0，main.js 重写配置 Hash 路由，4个页面路由：`/dashboard`、`/analysis`、`/burst`、`/bigscreen`。

2. **重写 App.vue 为全局导航壳层**：
   - 精美顶部导航栏（毛玻璃效果、粘性定位、活跃状态指示器）
   - 右侧「大屏模式」按钮和实时脉冲指示灯
   - 大屏模式下隐藏导航栏，显示返回按钮和时间
   - 页面切换过渡动画（fade + translateY）

3. **新建 BigScreen.vue 可视化大屏**：
   - 暗色炫酷风格（深海蓝 + 渐变背景 + 网格装饰）
   - 三栏布局：左侧热搜榜TOP25 + 中间趋势/热度图表 + 右侧ML分析
   - 支持点击热搜查看趋势、60秒自动刷新
   - 底部技术栈状态栏

4. **升级 Dashboard.vue**：渐变 Hero 区域、4色统计卡片（hover 动画）、渐变折线图、优化热搜榜样式

5. **升级 Analysis.vue**：统一按钮样式、双 Y 轴柱线图、美化数据表格（色彩标签）

6. **升级 BurstAnalysis.vue**：彩色等级标签（爆发/稳定/降温）、趋势方向箭头标签、聚类颜色标识、导出按钮优化

### 构建结果
- `npm run build` 成功，591 modules transformed，7.68s，exitCode 0
- 开发服务器运行于 http://localhost:5174/

---

## 大数据可视化三大新功能（第二次升级）

### 新增功能
1. **中国地图热力图**（评论省份分布）：
   - 新建 `SentimentMap.vue` 独立页面，包含 ECharts 地图组件
   - 从阿里云 DataV CDN 动态加载中国 GeoJSON 数据
   - 支持省份 hover tooltip、visualMap 热力渐变
   - 同时在 BigScreen 大屏中嵌入暗色风格地图

2. **热词词云**（ECharts wordcloud）：
   - 安装 `echarts-wordcloud` 包
   - 圆形词云布局，彩色随机配色，字体大小按频率映射
   - 普通页面（亮色）+ 大屏（暗色）两套风格
   - 关键词来自评论高频词，后端未接通时使用模拟数据

3. **情感分析统计**（正面/负面/中性）：
   - 4个统计卡片：正面/负面/中性/总量，含占比进度条
   - 环形饼图展示情感占比
   - 7天情感趋势折线图（正/负/中三线）
   - 情感关键词标签云（正面绿色/负面红色标签）
   - 省份 Top10 评论榜（渐变颜色进度条）

### 技术实现
- `api/index.js` 新增 `getCommentGeoData`、`getSentimentData`、`getWordCloudData` 三个接口
- 后端未实现时自动降级使用模拟数据（含省份分布、情感数据、词云数据）
- 新增路由 `/sentiment` → SentimentMap.vue
- App.vue 导航栏新增「🌍 评论情感分析」菜单项

### 构建结果
- `npm run build` 成功，599 modules transformed，8.64s，exitCode 0
- 开发服务器运行于 http://localhost:5175/

---

## 第五期：微博热搜情感分析与语义聚类增强

### 完成内容
1. **新增 sentiment/ 目录**（情感分析模块）：
   - `config.py` - 从 .env 读取情感分析配置
   - `data_loader.py` - 从 MySQL hot_search_raw 读取数据
   - `sentiment_analyzer.py` - 使用 SnowNLP 进行情感分析
   - `sentiment_job.py` - 情感分析主入口脚本
   - `mysql_writer.py` - 写入情感分析结果
   - `requirements.txt` - pandas, pymysql, python-dotenv, snownlp

2. **新增 semantic/ 目录**（语义聚类模块）：
   - `config.py` - 从 .env 读取语义聚类配置
   - `data_loader.py` - 从 MySQL 读取热搜标题数据
   - `embedding_cluster.py` - 核心语义聚类逻辑
   - `semantic_cluster_job.py` - 语义聚类主入口脚本
   - `mysql_writer.py` - 写入语义聚类结果
   - `model_cache/.gitkeep` - 本地模型缓存目录
   - `requirements.txt` - pandas, pymysql, python-dotenv, scikit-learn, jieba, sentence-transformers, openpyxl

3. **新增 sql/v5_sentiment_semantic.sql**：
   - `hot_search_sentiment_stats` - 情感分析单条结果表
   - `hot_search_sentiment_daily_stats` - 每日情感统计表
   - `hot_search_semantic_clusters` - 语义聚类结果表

4. **新增 backend/services/sentiment_semantic_service.py**：
   - `get_sentiment_summary()` - 情感分析概览
   - `get_sentiment_daily()` - 每日情感统计
   - `get_sentiment_top()` - 情感 Top 数据
   - `search_sentiment()` - 关键词搜索
   - `run_sentiment_job()` - 触发情感分析任务
   - `get_semantic_clusters()` - 语义聚类结果
   - `get_semantic_cluster_summary()` - 语义主题分布
   - `run_semantic_cluster_job()` - 触发语义聚类任务
   - `export_enhanced_report_csv()` - 导出增强版 CSV 报告
   - `export_enhanced_report_excel()` - 导出增强版 Excel 报告

5. **新增 backend/routes/sentiment_semantic.py**：
   - `/api/sentiment/summary` - GET
   - `/api/sentiment/daily` - GET
   - `/api/sentiment/top` - GET
   - `/api/sentiment/search` - GET
   - `/api/sentiment/run` - POST
   - `/api/semantic/clusters` - GET
   - `/api/semantic/clusters/summary` - GET
   - `/api/semantic/run` - POST
   - `/api/export/enhanced_report.csv` - GET
   - `/api/export/enhanced_report.xlsx` - GET

6. **新增 frontend/src/views/SentimentAnalysis.vue**：
   - 情感概览卡片（平均情绪分、正向/中性/负向数量）
   - 情感分布饼图
   - 公众情绪指数折线图
   - 情绪 Top20 表格（支持搜索和标签筛选）
   - 语义聚类分布图
   - 语义聚类明细表
   - 运行任务按钮（情感分析、语义聚类）
   - 导出报告按钮（CSV、Excel）

7. **修改文件**：
   - `.env.example` - 追加第五期配置
   - `backend/main.py` - 引入新路由
   - `backend/requirements.txt` - 添加 snownlp, sentence-transformers
   - `frontend/src/api/index.js` - 新增 10 个 API 方法
   - `frontend/src/main.js` - 新增 `/sentiment-analysis` 路由
   - `frontend/src/App.vue` - 导航栏新增「💡 情感语义分析」菜单
   - `README.md` - 全面更新第五期说明

### 技术要点
- SnowNLP 用于中文情感分析，score >= 0.6 为正向，<= 0.4 为负向
- sentence-transformers 默认使用 paraphrase-multilingual-MiniLM-L12-v2 模型
- 如果 sentence-transformers 不可用，自动回退到 TF-IDF + KMeans
- 支持配置 SEMANTIC_LOCAL_MODEL_PATH 使用本地模型
- KMeans 聚类支持动态调整聚类数量（样本不足时）

---

### 10. 部署文档创建

创建了 `DEPLOYMENT.md`，包含：
- 系统要求（硬件/软件/网络）
- 快速部署步骤（8 步完成）
- 详细安装说明（数据库、Python、前端）
- 完整配置说明（所有 .env 配置项及含义）
- 启动方式（后端、前端、采集器、分析任务）
- 验证方法（数据库、API、前端页面）
- 常见问题 FAQ（8 个常见问题及解决方案）
- 项目架构图（系统架构、数据流程、技术栈）
- 完整文件结构说明

---

### 11. 前端整体美化（科技蓝主题统一）

#### 修改范围
重写了全部 7 个前端文件，保持 script 逻辑和 API 调用完全不变：
- `frontend/src/App.vue` - 全局 CSS 变量 + 导航栏
- `frontend/src/views/Dashboard.vue` - 首页实时总览
- `frontend/src/views/Analysis.vue` - PySpark 分析页
- `frontend/src/views/BurstAnalysis.vue` - ML 爆发趋势页
- `frontend/src/views/SentimentMap.vue` - 评论情感分析页
- `frontend/src/views/SentimentAnalysis.vue` - 情感语义分析页
- `frontend/src/views/BigScreen.vue` - 可视化大屏

#### 设计规范
- **主色调**：--primary: #2563eb（科技蓝），--accent: #06b6d4（青色）
- **辅助色**：--success: #22c55e，--warning: #f59e0b，--danger: #ef4444
- **背景**：body 使用线性渐变 #f0f5ff → #f8fafc → #f1f5f9
- **卡片**：玻璃拟态风格（rgba(255,255,255,0.92) + backdrop-filter）
- **Hero 区**：蓝色渐变背景（#2563eb → #1d4ed8 → #1e40af）
- **图表**：统一使用蓝色系（#2563eb 为主），去掉橙红色系
- **布局**：所有页面 max-width: 1440px，padding: 24px

#### 关键改动
1. **去掉所有 emoji**，替换为 Lucide 风格 SVG 图标
2. **导航栏优化**：左侧系统名称（去掉毕业设计标签），中间菜单居中，右侧实时状态灯 + 大屏按钮
3. **统计卡片**：统一 4 色 top border + icon wrap 背景色区分
4. **热搜榜**：保留金银铜徽章，但使用新的渐变配色
5. **标签系统**：level-tag、trend-tag、cluster-badge 统一圆角 pill 样式
6. **响应式**：grid 布局 + @media 查询，支持移动端

#### 构建结果
- `npm run dev` 成功启动，Vite 服务正常运行

---

### 12. 前端可视化深度优化（数据洞察型升级）

#### 目标
将前端从"数据展示型页面"优化为"数据洞察型可视化页面"，增加图表多样性、分析结论、动态变化。

#### 后端新增
- `backend/services/visual_service.py` - 可视化增强查询服务
  - `get_hourly_heatmap()` - 按日期和小时分组的热搜采集热力图数据
  - `get_rank_movers()` - 最新两批热搜之间的排名变化榜（上升/下降 Top10）
  - `get_insights()` - 自动生成数据洞察（总记录数、榜首话题、高爆发数、情感倾向等）
- `backend/routes/visual.py` - 新增路由
  - `GET /api/visual/hourly-heatmap`
  - `GET /api/visual/rank-movers`
  - `GET /api/visual/insights`
- `backend/main.py` - 注册 visual_router

#### 前端新增通用组件
- `frontend/src/components/EmptyState.vue` - 通用空状态组件

#### 前端 API 新增
- `getHourlyHeatmap()` / `getRankMovers()` / `getVisualInsights()`

#### 各页面优化详情

**Dashboard.vue 首页**：
- 新增「热搜活跃时间热力图」（ECharts heatmap，日期×小时×热搜数量）
- 新增「排名变化榜」（上升最快 Top10 + 下降最快 Top10）
- 新增「自动洞察卡片」（根据 summary/ranking/trend 生成 3-4 条分析结论）
- 趋势图与热力图并排双栏布局

**Analysis.vue PySpark 分析页**：
- 新增「高频关键词词云图」（echarts-wordcloud，Top 100 关键词）
- 新增「数据分析说明卡片」（自动生成 3 条分析结论：高频关键词、采集量最高日、最高热度日）
- 每日采集统计改为柱状图（total_records）+ 折线图（total_keywords）组合图
- 关键词柱状图与词云并排双栏

**BurstAnalysis.vue 爆发趋势页**：
- 新增「爆发趋势气泡散点图」（x=热度变化率, y=排名变化, 大小=当前热度, 颜色=爆发等级）
- 新增「爆发等级环形图」（高/中/低爆发数量占比）
- 新增「机器学习洞察卡片」（对 Top3 高爆发话题生成解释文字）
- 保留原有概率柱状图和趋势方向饼图

**SentimentAnalysis.vue 情感语义分析页**：
- 新增「公众情绪指数仪表盘」（ECharts gauge，0-100，分正向/中性/负向区间）
- 新增「情感-热度气泡图」（x=情感分数, y=热度值, 大小=热度值）
- 新增「语义聚类 Treemap」（一级=主题类别, 二级=关键词, 大小=热度值）
- 新增「语义主题卡片墙」（每个主题显示数量、平均热度、最高热度、3个代表关键词）
- 新增「情感分析洞察卡片」（整体情绪倾向、正/负占比、情绪分最高话题）
- 4 图表并排布局（仪表盘+饼图+折线图+气泡图）

**BigScreen.vue 大屏**：
- 顶部大数字卡片新增「平均情绪分」和「高爆发话题数」
- 右列新增「爆发趋势气泡图」
- 底部状态栏新增「上次刷新时间」
- 数据加载增加情感概览和高爆发统计

#### 构建结果
- `npm run dev` 成功启动
- `npm run build` 成功，603 modules transformed，8.47s，exitCode 0
